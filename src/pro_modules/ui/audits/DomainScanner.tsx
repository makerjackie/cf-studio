export function DomainScanner(_props: { onNavigate?: (id: string) => void }) {
  return <div className="text-sm text-muted-foreground">Domain scanner is not included in this public fork yet.</div>;
}
